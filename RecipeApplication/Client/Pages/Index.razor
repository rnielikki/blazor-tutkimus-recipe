@page "/"
@inject KeywordSearchBox.ISearchModel<RecipeApplication.Models.RecipeDto> SearchModel;

<Search>
</Search>

@code{
    protected override void OnInitialized()
    {
        if (SearchModel is KeywordSearchBox.SearchModel<RecipeApplication.Models.RecipeDto> model) {
            model.RefreshActions -= StateHasChanged;
            model.RefreshActions += StateHasChanged;
        }
    }
}

